apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: graylog-data-pvc
  namespace: logistic-package
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi

